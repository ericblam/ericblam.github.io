Run the following to build the resume within the `_resume` directory:

```
$ xelatex resume.tex 
```

To create a printable (black and white) version, change `\documentclass[]{friggeri-cv}` in `resume.tex` to `\documentclass[print]{friggeri-cv}`